spring:
  application:
    name: my-shop-user
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://81.71.147.52:3306/springcloud?characterEncoding=UTF8&useSSL=false
    username: root
    password: bzx-MySQL1
  jpa:
    show-sql: true
    generate-ddl: true
    database: mysql
  rabbitmq:
    host: 81.71.147.52
    port: 5672
    username: guest
    password: guest
    virtual-host: /

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8088/eureka/
  instance:
    prefer-ip-address: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
